<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Us</title>
    
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="/css/contact.css">
</head>
<body>
    <!-- Hero Section -->
    <header class="hero">

        <!-- Navigation Bar -->
        <nav class="navbar">
            <div class="logo">AutoCare</div>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/services">Services</a></li>
                <li><a href="/contacts">Contact</a></li>
            </ul>
            <div class="burger">
                <div></div>
                <div></div>
                <div></div>
            </div>
        </nav>

        <div class="overlay">
            <h1>Contact Us</h1>
            <p>Committed to Keeping You on the Road.</p>
        </div>
    </header>

<!--........................ Start Content........................-->
        <div class="contain">
            <div class="item">
                <div class="contact">
                    <div class="first-text">Let's get in touch</div>
                    <img src = "/images/contact.png" alt = "" class="image">
                    <div class="social-links">
                        <br>
                        <span class="second-text">Connect with us:</span>
                        <ul class="social-media">
                            <li> <a href = "https://web.facebook.com"><i class='bx bxl-meta' ></i></a></li>
                            <li> <a href = "https://www.youtube.com"><i class='bx bxl-youtube' ></i></a></li>
                            <li> <a href = "https://www.instagram.com"><i class='bx bxl-instagram' ></i></a></li>
                            <li> <a href = "https://mail.google.com"><i class='bx bxl-gmail' ></i></a></li>

                        </ul>
                    </div>
                </div>
            
        
                <div class="submit-form">
                        <h4 class = "third-text text">Contact Us</h4>
                        <form  action="/api/contact" method="POST" class="forms">
                            <div class="input-box">
                                <input type = "text" name="name" class = "input" required>
                                <label for = "">Name</label>
                            </div>
                            <div class="input-box">
                                <input type = "email" name="email"class = "input" required>
                                <label for = "">Email</label>
                            </div>
                            <div class="input-box">
                                <input type = "tel" name="phone" class = "input" required>
                                <label for = "">Phone</label>
                            </div>
                            <div class="input-box">
                                <textarea name = "message" class = "input" required id ="message" cols ="30" rows="10"></textarea>
                                <label for = "">Message</label>
                            </div>
                            <input type="submit" class ="btn" value="Submit">
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <div id="alertModal" class="modal">
            <div class="modal-content">
                <span class="close-button">&times;</span>
                <p id="modalMessage"></p>
            </div>
        </div>
  
        <section class="cta">
            <div class="container">
                <h2>Empowering Your Drive with Confidence.</h2>
                <p>
                    Discover the AutoCare difference—where quality meets reliability. 
                    Join us in taking care of your vehicle and ensuring a safe and enjoyable driving experience. We look forward to serving you!
                </p>
            </div>
        </section>

<!--........................Footer........................-->   <!-- Footer Section -->
        <footer>
            <div class="container">
                <p>Copyright &copy; 2024, IT students. All Rights Reserved.</p>
                <nav>
                    <a href="/">Home</a> |
                    <a href="/about">About</a> |
                    <a href="/services">Services</a> |
                    <a href="/contacts">Contact</a>
                </nav>
            </div>
        </footer>



        <script>
             document.addEventListener("DOMContentLoaded", function () {
    const contactForm = document.getElementById("contactFormForm");  // Ensure you target the correct form ID
    const alertModal = document.getElementById("alertModal");
    const modalMessage = document.getElementById("modalMessage");
    const closeButton = document.querySelector(".close-button");

    // Handle form submission
    contactForm.addEventListener("submit", async (e) => {
        e.preventDefault();  // Prevent the default form submission

        const formData = new FormData(contactForm);  // Create FormData object from the form
        const formObj = Object.fromEntries(formData.entries());  // Convert FormData to a plain object

        console.log("Form data being sent:", formObj);  // Debugging step to check what data is being sent

        try {
            const response = await fetch("/api/contact", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json",
                },
                body: JSON.stringify(formObj),  // Send the form data as JSON
            });

            const result = await response.json();

            if (response.ok) {
                modalMessage.textContent = result.message || "Email sent successfully!";
            } else {
                modalMessage.textContent = result.message || "Failed to send email.";
            }

            // Display the modal with the result message
            alertModal.style.display = "block";
        } catch (error) {
            console.error("Error:", error);
            modalMessage.textContent = "An error occurred while sending the email.";
            alertModal.style.display = "block";
        }
    });

    // Close modal
    closeButton.addEventListener("click", () => {
        alertModal.style.display = "none";
    });

    // Close modal when clicking outside of it
    window.addEventListener("click", (event) => {
        if (event.target === alertModal) {
            alertModal.style.display = "none";
        }
    });
});

        </script>

        <script>
            document.addEventListener("DOMContentLoaded", function () {
                const burger = document.querySelector(".burger");
                const navLinks = document.querySelector(".nav-links");
        
                burger.addEventListener("click", () => {
                    navLinks.classList.toggle("active");
                });
            });
        </script>

        <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>